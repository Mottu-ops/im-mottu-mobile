import 'package:flutter/material.dart';

abstract class AppColors {
  static const redTheme = Color.fromRGBO(220, 45, 45, 1);
  static const hintColor = Colors.grey;
  static Color fillColor = Colors.grey.shade200;
  static const greyFont = Color.fromRGBO(105, 112, 141, 1);
  static const overlay = Color.fromRGBO(0, 0, 0, 0.35);
  static const whiteTheme = Color.fromRGBO(254, 254, 254, 1);
  static const divider = Color.fromRGBO(0, 0, 0, 0.1);
  static const overlayWhite = Color.fromRGBO(255, 255, 255, 0.6);
}
